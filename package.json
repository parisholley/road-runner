{
  "name": "@parisholley/road-runner",
  "author": {
    "name": "Paris Holley",
    "email": "mail@parisholley.com",
    "url": "https://www.parisholley.com"
  },
  "description": "The fastest path resolver for HTTP routing on modern Node.JS",
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/node": "^12.7.4",
    "coveralls": "^3.0.6",
    "jest": "^24.9.0",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.3.0",
    "typescript": "^3.6.2"
  },
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "preversion": "rm -rf dist && tsc",
    "publish": "git push --tags",
    "test": "jest",
    "test-coverage": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "engines": {
    "node": ">=6"
  },
  "version": "1.0.6"
}
